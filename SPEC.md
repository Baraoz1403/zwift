# מסמך אפיון — AI Coaching System for Zwift

גרסה 0.1 · טיוטה לאישור · יולי 2026

---

## ויז'ן

מערכת AI שמתפקדת כמאמן אישי לרכיבה.
היא מכירה כל מתאמן לעומק, מתכננת אימונים מקצועיים, ומעלה אותם ישירות לזוויפט.
המתאמן עולה לאופניים ומוצא אימון שמחכה לו — מותאם, מנומק, ומבוסס על הנתונים האחרונים שלו.

---

## משתמשים

- בראק + חברים (קבוצה קטנה, אישי, לא מסחרי)
- גישה דרך browser — מובייל ו-iPad בעיקר
- אין תשלום, אין שיווק בשלב זה

---

## קלט — מה המערכת צריכה לדעת על כל מתאמן

### פרופיל בסיסי (חד-פעמי, מתעדכן לפי בקשה)
- שם
- FTP (וואט)
- משקל (לחישוב W/Kg)
- מטרות (הפחתת משקל / שיפור FTP / כושר כללי / כיף)
- ימי אימון זמינים בשבוע (1–6)
- רמת ניסיון (מתחיל / בינוני / מתקדם)

### נתוני כושר שוטפים (מ-Intervals.icu)
- CTL (כושר כרוני)
- ATL (עומס חריף)
- TSB (מאזן עומס)
- היסטוריית אימונים אחרונים

### פידבק מתאמן (אחרי כל אימון)
- איך הרגיש (1–5 או טקסט חופשי)
- האם סיים / לא סיים
- הערות

---

## זרימה מרכזית

```
פרופיל מתאמן + נתוני ICU + פידבק שבועי
        ↓
   AI מייצר תוכנית שבועית
        ↓
   בדיקת quality gate (קוד — לא AI)
        ↓
   העלאה ל-Intervals.icu
        ↓
   סנכרון אוטומטי לזוויפט
        ↓
   מתאמן רוכב
        ↓
   פידבק → מזין את התוכנית הבאה
```

---

## הגדרת "אימון מקצועי" — מה מותר ומה אסור

### מותר
- כל אימון = warmup + intervals + cooldown (בלוקים מדויקים)
- intervals עם ערכי וואט ספציפיים (% מ-FTP)
- שם שמתאר את האימון הפיזיולוגית
- ימי מנוחה מוגדרים (Recovery / Rest)

### אסור
- "Foundation Ride" — רכיבה חופשית ללא מבנה
- Z2 ביום אימון רגיל (מותר רק כ-cooldown/warmup)
- אימון ללא structure blocks
- בחירת תבנית מוכנה ושינוי ערכים — כל אימון = יצירה חדשה

### Quality Gate (קוד, לא הבטחות)
- כל יום אימון (לא Recovery) חייב warmup + intervals + cooldown
- לפחות 3 intervals blocks ביום אימון
- TSS שבועי בטווח הגיוני לפי CTL+TSB
- Foundation Ride ביום אימון = דחייה ו-retry אוטומטי

---

## Integrations

| שירות | מה | איך |
|---|---|---|
| Intervals.icu | העלאת אימונים, קריאת CTL/ATL/TSB | API key אישי |
| Zwift | קבלת האימונים | סנכרון אוטומטי מ-ICU |
| Zwift API | קריאת היסטוריית רכיבות | Bearer token |

---

## תקשורת מתאמן-מאמן

- **צ'אט חופשי** — שאלות, הערות, בקשות שינוי
- **שאלון אחרי אימון** — איך הרגשת, האם סיימת
- **שניהם מזינים את התוכנית הבאה** — לא תצוגה בלבד

---

## מה לא בגרסה הראשונה

- אין תשלום / subscription
- אין אדמין לניהול משתמשים (הכל ידני בשלב זה)
- אין dashboard מפואר — ממשק פשוט ופונקציונלי
- אין TrainingPeaks integration

---

## שאלות פתוחות לאישור

1. **onboarding**: מתאמן חדש מגיע — ממלא פרופיל בעצמו, או בראק מגדיר לו?
2. **שינוי תוכנית**: מתאמן רוצה לשנות אימון ביום ספציפי — האם ה-AI מסביר ומאפשר?
3. **תדירות יצירת תוכנית**: כל שבוע אוטומטית, או לפי בקשה?

---

*מסמך זה ממתין לאישור לפני כל שורת קוד.*
