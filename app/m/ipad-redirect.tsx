"use client";

/**
 * IpadRedirect — disabled. All devices now use /m/* directly.
 * Tablet routing removed; this component is a no-op kept to avoid import errors.
 */
export default function IpadRedirect() {
  return null;
}
