/**
 * Tablet — Edit Training Profile
 * Reuses the mobile profile editor inside the tablet shell (sidebar + layout).
 * Accessible at /tablet/profile/edit — the sidebar remains visible during editing.
 */
export { default } from "@/app/m/profile/edit/page";
