/**
 * Tablet — Coach tab
 * Embeds the mobile coach page content. The mobile coach page is a client
 * component that handles plan generation, feedback, and AI chat. Rather than
 * duplicating that logic, we render it here inside the tablet shell.
 */
export { default } from "@/app/m/coach/page";
